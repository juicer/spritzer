<link href="/app/assets/stylesheets/bootstrap.css" rel="stylesheet" type="text/css">
<div class="navbar">
  <div class="navbar-inner">
    <div class="container">
      <a class="brand" href="/">Spritzer</a>
      <ul class="nav">
      <% @environments.each do |environment| %>
      <% if @active == environment %>
      <li class="active"><a href="/<%= environment %>/"><%= environment %></a></li>
      <% else %>
      <li><a href="/<%= environment %>"><%= environment %></a></li>
      <% end %>
      <% end %>
      <form class="navbar-search" method="post" action="/<%= @active %>/package/search/1">
      <input type="text" class="search-query" id="package" name="package" placeholder="Package Search" />
      </form>
      </ul>
    </div>
  </div>
</div>
<div class="container-fluid">
  <div class="row-fluid">
    <div class="span2">
      <% if @repos.length != 0 %>
      <ul class="nav nav-tabs nav-stacked">
      <% @repos.each do |repo| %>
      <li><a href="http://pulp-gca01.util.phx1.redhat.com/pulp/repos/<%= @active %>/<%= repo['display_name'] %>"><%= repo["display_name"] %></a></li>
      <% end %>
      <% end %>
      </ul>
    </div>
    <div class="span10">
      <h3>Package Search:</h3>
      <% if @packages.length != 0 %>
      <table class="table-striped" cellpadding="10" width="70%">
	<tr>
	  <td><b>Package</b></td>
	  <td><b>Repos</b></td>
	</tr>
	<% @packages.each do |package| %>
	<tr>
	  <td><%= package["filename"] %></td>
	  <td>
	    <% package["repos"].each do |repo| %>
	    <a href="http://pulp-gca01.util.phx1.redhat.com/pulp/repos/<%= @active %>/<%= repo %>/<%= package['filename']%>"><%= repo %></a>
	    <% end %>
	  </td>
	</tr>
	<% end %>
	<% end %>
	</table>
    </div>
  </div>
</div>
